import React, {useContext, useEffect, useState} from 'react'

export const ShopContext = createContext(null);

function Shopcontext({products}) {
  return (
    <div></div>
  )
}

export default Shopcontext